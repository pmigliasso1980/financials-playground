node_modules/
.env
.env.local
dist/
.backfill-checkpoint.json
.backfill-deals.json

# Data harvested from EDGAR — regenerated with `npm run harvest`
harvest/out/

# The fixtures ARE versioned: they are real documents, trimmed, which make the
# parsing test reproducible. Captured with `npm run harvest:capture`.
!harvest/fixtures/
src/generated/
*.log
.DS_Store

# Corpus snapshots, for comparing between runs.
.snapshots/
out/
